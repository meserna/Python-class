import csv
with open('device.csv') as csvfile
  testreader = csv.reader(csvfile, delimiter = ' ', quotechar= '|'
  print row[1]
  print row[2]
  
  example 2
  for line_number, row in zip(range(1), csvfile)
    print row
